# Test-only dependencies for the HeritEdge backend
# Install with: pip install -r requirements-test.txt
#
# These supplement (not replace) requirements.txt
pytest>=7.4.0
pytest-html>=4.1.1
pytest-cov>=4.1.0
httpx>=0.25.0  # already required by FastAPI TestClient
